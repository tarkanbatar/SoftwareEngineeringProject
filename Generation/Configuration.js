class Configuration{
    constructor(seed, octaves, persistence, smoothness){
        this.seed = seed
        this.octaves = octaves
        this.persistence = persistence
        this.smoothness = smoothness
    }
}